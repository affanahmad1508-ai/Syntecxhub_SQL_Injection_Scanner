# SQL Injection Scanner

A simple Python-based SQL Injection vulnerability scanner that tests HTML forms using basic payloads and detects common SQL error messages.

## Features
- Form discovery
- GET and POST support
- SQL error detection
- Session handling

## Installation

```bash
pip install -r requirements.txt
